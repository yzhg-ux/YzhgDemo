<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 进度条背景 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="3dp" />
            <gradient
                android:angle="270"
                android:centerColor="@color/color_F1F1F1"
                android:endColor="@color/color_F1F1F1"
                android:startColor="@color/color_F1F1F1" />
        </shape>
    </item>

    <!-- 蓝色的进度值 -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="3dp" />
                <gradient
                    android:centerColor="@color/color_258FFA"
                    android:endColor="@color/color_258FFA"
                    android:startColor="@color/color_258FFA" />
            </shape>
        </clip>
    </item>

</layer-list>